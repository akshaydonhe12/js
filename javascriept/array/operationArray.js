// operationArray  / what we do in that  

let book = ["Math", "History", "Bio", "Cs"];

// let lang = ["hindi", "english"];
// let newBook = book.concat(lang); //.concat used for multiple array join

let bookPages = [
    ["math", "200"],
    ["Bio", "400"],
    ["history", "500"],
    ["cs", "600"],
   
];// Multidimension Array information store

let fetch = bookPages[1];

//let book ="math";  // to find it is actully an array used in console Array.isArray(variable name)

//book.unshift("Hindi"); // .push add elements // .unshift use for add elemets on start

//book.splice(1,1); // delete last element // .shift delete start elemts // .splice delted middle elemts take 2 paramenters 

//book.length = 0; // empty blank

//let position = book.indexOf("Bio"); // .indexOf find position of elements


//let text = "this is a elelmets";

//let wordarray = text.split(' '); // to word convert in array used .split // .join use for all elements joints.

console.log(fetch); //.length total elemets check